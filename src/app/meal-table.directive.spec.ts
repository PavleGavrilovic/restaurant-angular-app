import { MealTableDirective } from './directives/meal-table.directive';

describe('MealTableDirective', () => {
  it('should create an instance', () => {
    const directive = new MealTableDirective();
    expect(directive).toBeTruthy();
  });
});
