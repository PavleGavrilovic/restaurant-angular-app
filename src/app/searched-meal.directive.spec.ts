import { SearchedMealDirective } from './directives/searched-meal.directive';

describe('SearchedMealDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchedMealDirective();
    expect(directive).toBeTruthy();
  });
});
