import { SearchedMealDirectiveDirective } from './directives/searched-meal.directive';

describe('SearchedMealDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new SearchedMealDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
